// Fixed values until the settings window is wired up in phase 4.

// Play mode ("Játék mód"): fling physics and motion face reactions
export const PLAY_MODE: boolean = true;

// Reading animation during focus
export const READING_ANIMATION: boolean = false;

// Pomodoro lengths (minutes); a running session keeps the values it started with
export const POMODORO_FOCUS_MIN = 25;
export const POMODORO_BREAK_MIN = 5;

// Duration offered first in pickDur; later choices are remembered for the session
export const DEFAULT_DURATION_MIN = 25;
